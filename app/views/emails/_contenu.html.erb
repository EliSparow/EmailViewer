<div class="container contenu">
  <%= link_to " Delete", email_path(@email), class: "btn btn-sm btn-warning bouton", remote: true,  method: :delete, data: { confirm: "Are you sure?" } %>

  <h3><%= @email.object %></h3>
  <hr>
  <p><%= @email.body %></p>
</div>
